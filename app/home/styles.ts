import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: 60,
    paddingHorizontal: 12,
    alignItems: "center",
  },
  header: {
    paddingHorizontal: 8,
    marginBottom: 20,
  },
});
